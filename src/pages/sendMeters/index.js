import React from 'react';
import SendMetersComponent from '../../components/dashboard/sendMeters';

import './send-meters.scss';

const SendMeters = () => {
  return (
    <div className="page-send-meters">
      <SendMetersComponent />
    </div>
  );
};

export default SendMeters;
